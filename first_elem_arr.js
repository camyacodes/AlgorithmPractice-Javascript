//Return the First Element in an Array
//Create a function that takes an array containing only numbers and return the first element (e.g. getFirstValue([1, 2, 3]) ➞ 1)

function getFirstValue(arr) {
    return arr[0]
}

console.log(getFirstValue([10, 2, 3]))